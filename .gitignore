node_modules
dist
dist-ssr
*.local
*.log

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Throwaway scripts
scripts/_scratch/

# Full Phase 0 network log — large, and the artifacts that matter are copied
# up into manifest/ and documented in manifest/README.md.
manifest/_netlog/

# Per-source capture cache — regenerated by `npm run capture`.
captures/by-source/
